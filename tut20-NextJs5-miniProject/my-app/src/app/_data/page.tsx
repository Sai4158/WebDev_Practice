import React from "react";

const page = () => {
  return <div>you can not see this in the web page</div>;
};

export default page;

//this is a private floder, you cannot access it if you put underscore before you start the folder name
